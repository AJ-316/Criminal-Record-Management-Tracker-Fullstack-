VITE_API_URL=http://localhost:8081

# Example: when running frontend dev server (vite) on default port 5173, set VITE_API_URL to the backend base URL.
# Then run: npm run dev (or pnpm/dev) and the frontend will use this URL to call the backend API.
